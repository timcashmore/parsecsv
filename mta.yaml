_schema-version: "3.2"
ID: parsecsv
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: parsecsv-destination-content
  type: com.sap.application.content
  requires:
  - name: parsecsv-destination-service
    parameters:
      content-target: true
  - name: parsecsv-repo-host
    parameters:
      service-key:
        name: parsecsv-repo-host-key
  - name: parsecsv-uaa
    parameters:
      service-key:
        name: parsecsv-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: parsecsv_html_repo_host
          ServiceInstanceName: parsecsv-html5-srv
          ServiceKeyName: parsecsv-repo-host-key
          sap.cloud.service: parsecsv
        - Authentication: OAuth2UserTokenExchange
          Name: parsecsv_uaa
          ServiceInstanceName: parsecsv-xsuaa-srv
          ServiceKeyName: parsecsv-uaa-key
          sap.cloud.service: parsecsv
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: parsecsv-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: parsecsv-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - parsecsv.zip
      name: parsecsv
      target-path: resources/
- name: parsecsv
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: parsecsv-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: parsecsv-destination-service
    service-plan: lite
- name: parsecsv-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: parsecsv-xsuaa-srv
    service-plan: application
- name: parsecsv-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: parsecsv-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
